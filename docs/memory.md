# Project memory — Phase 12 complete (all roadmap phases implemented)

- Prior stabilization task: complete the authoritative [2026-09-09 audit](current-project-review-2026-09-09.md), not a new phase. No Phase 7, providers, databases or Phase 1–3 redesign. Track remaining work in [stabilization status](stabilization-implementation-status.md).
- This is a local synthetic-data prototype; clinical content/translations are unvalidated. No diagnosis, prescribing, production-readiness or complete de-identification claims.
- React/Vite/TypeScript frontend; FastAPI/Pydantic/SQLAlchemy/Alembic backend; existing PostgreSQL on 55432, app DB medikiosk and isolated medikiosk_test. Frontend 5175, API 8010. Use backend/.venv and scripts. Secrets/.runtime/uploads are ignored.
- Preserve pinned deterministic flow snapshots, optimistic interview revisions, retry receipts, append-only answers, optional immutable normalization snapshots and immutable confirmed summaries.
- Staff routes reuse server-side active demo-doctor identity, gated by DEMO_MODE outside production. This is not production authentication. Triage and document reads/reviews require the doctor dependency. Client-supplied reviewer names are rejected. Public session detail excludes added staff document/alert payloads; original Phase 1 patient UUID access remains a local-demo limitation.
- WebSocket admission uses authenticated REST-issued, single-use 30-second tickets and an origin check. The selected subprotocol is medikiosk. Vite must proxy WebSockets; uvicorn needs the locked websockets transport. Socket delivery is process-local, not guaranteed human notification. Kiosk asks patients to contact staff directly.
- Alert status remains new/acknowledged/resolved for compatibility; trigger_active and acknowledgement_state are explicit API projections. Resolution preserves acknowledgement history. Reactivation clears current acknowledgement and increments evidence revision; audit events retain transitions. Staff acknowledgement must match expected_revision.
- Existing rule thresholds unchanged; descriptions only state checked conditions. Current-symptom concepts are scoped to source fields, present polarity and certain status. Existing exact multilingual fixtures provide a deterministic offline path. Sputum matching uses explicit positive demo phrases, not substring inference. Unrecognized text does not establish absence; manual review remains necessary.
- Documents: content validation using Pillow/pypdf, bounded sizes/pages/pixels, resolved path containment, compensating file cleanup on failed DB persistence. Mock extraction only for content-addressed files in ai/document_fixtures; arbitrary uploads have unavailable OCR. observations is the canonical lab key; missing flags stay null. Review versions and audit history protect concurrent changes; confirmed/cancelled sessions are locked.
- Voice: server checks consent/current free-text question before provider invocation. Signed candidates bind text/session/question/language/revision for 10 minutes; restart invalidates unconfirmed candidates. Confirmed answer transaction records candidate/provider/model provenance. Edited candidates are typed. Multipart can spool audio to temporary disk; UploadFile closes on all route outcomes. No deliberate permanent audio storage; abrupt process termination cannot be described as guaranteed cleanup.
- Consented browser recordings are converted in memory to 16-kHz mono PCM16 WAV before upload; no recording is retained by this conversion. Sarvam REST ASR/TTS uses pinned `sarvamai==0.1.31a4`, zero SDK retries, a 12-second provider timeout inside the 15-second application deadline, and BCP-47 language mapping for en/bn/hi. A synthetic Bengali TTS-to-ASR loopback succeeded with a signed candidate, establishing provider transport but not physical microphone/accent accuracy. Mock/unavailable TTS uses a visibly labelled browser speech fallback. BHASHINI remains unconfigured. NVIDIA's latest single application request returned `unavailable/timeout` after about 8.1 seconds.
- Schema head `8b4e9c2d1f73` includes Phase 11 ABDM/HIS state; Phase 12 requires no migration. `verify-stabilization-migrations.py` remains the historical Phase 4–7 preservation verifier.
- Windows Application Control blocks `pg_ctl.exe`; the PostgreSQL Windows service was disabled and stopped during the latest audit. No policy or service setting was changed. `-UseRunningDatabase` starts only app services when PostgreSQL is already running. PostgreSQL/database-restart acceptance remains pending.
- Never commit .env, runtime data/logs/uploads, candidate tickets or provider credentials.

- Latest verification (2026-09-11): 364 SQLite backend tests, 98 frontend component tests, and 29 Chromium E2E journeys pass; Ruff/ESLint/Prettier/TypeScript build pass. Browser E2E covers the one-click Phase 12 showcase and fixture document upload-to-preview journey. PostgreSQL/database-process restart checks remain pending because the database service is disabled and stopped.

- Phase 8 summary drafting, revision audit, and clinician confirmation implemented.
- Phase 9 field verification hardening, clinical amendments with audit addenda, and cross-referencing implemented.
- Phase 10 HL7 FHIR R4 export architecture implemented: decoupled on-demand adapter layer (`FHIRAdapterService`), pure Pydantic v2 schemas (`base.py`, `resources.py`, `bundle.py`), `document` (LOINC 34105-7 Composition at `entry[0]`) and `collection` bundles, non-diagnostic guardrails (`verificationStatus: provisional`), doctor UI export modal (`FHIRExportModal.tsx`), full test suites passing (345 backend, 79 frontend).
- Phase 11 ABDM and HIS interoperability demonstration implemented: ABDM sandbox & mock gateway engine (`ABDMService`), M1 ABHA identity verification, M2 HIP Care Context Linking, M3 FHIR R4 Document bundle data exchange, outbound Hospital Information System dispatcher (`HISService`) with verifiable receipts, kiosk inline ABHA verification, doctor workspace `ABDMHISModal`, dedicated persistence (`abdm_records` table, migration `8b4e9c2d1f73`), and full test suites passing (350 backend, 84 frontend).
- Phase 12 demo polish implemented (final roadmap phase): `ShowcaseService` with a complete 26-answer pinned Bengali chest-pain flow, deterministic RF-CHEST-001/RF-CHEST-002 alerts, two stored content-addressed mock document fixtures with review provenance, ABDM/HIS mock state, and a revisioned 10-section clinical summary; typed demo APIs/CLI; rollback/reset storage cleanup; synchronized fullscreen fallback; doctor/kiosk showcase controls; 356 SQLite backend and 98 frontend tests passing.
