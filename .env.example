# Backend configuration belongs in backend/.env; copy backend/.env.example.
# scripts/setup-postgres.ps1 generates local PostgreSQL credentials automatically.
# Frontend configuration belongs in frontend/.env; the default /api uses Vite's proxy.
# No external credentials are needed through Phase 3A; normalization uses a deterministic mock.
