# Backend configuration belongs in backend/.env; copy backend/.env.example.
# Configure the backend-only Supabase URL in backend/.env.
# Frontend configuration belongs in frontend/.env; the default /api uses Vite's proxy.
# Provider selection and optional credential names are documented in backend/.env.example.
