# Windows CPU compatibility overlay for upstream commit
# fc8515f9aa9dc54a93a75366ca8d8ac4adaf35c8.
#
# Upstream uses tensorflow[and-cuda]==2.19.0 on every non-macOS host. That
# extra requires NVIDIA NCCL, which has no compatible native Windows wheel and
# is unnecessary on this Intel-only machine. All other upstream pins are kept.
levenshtein==0.27.4
mlflow==3.16.0
openai==3.8.0
opencv-python-headless==5.0.0.93
tensorflow==2.19.0

# Standalone local boundary and tests; isolated from the MediKiosk backend.
fastapi>=0.115,<1
python-multipart>=0.0.20,<1
uvicorn>=0.34,<1
pytest>=8,<10
httpx>=0.27,<1
